meta {
  name: Update Symptom
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/symptoms/:id
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Updated Symptom",
    "is_active": false
  }
}
