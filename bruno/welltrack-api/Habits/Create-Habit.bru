meta {
  name: Create Habit
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/habits
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Meditation",
    "tracking_type": "boolean"
  }
}
