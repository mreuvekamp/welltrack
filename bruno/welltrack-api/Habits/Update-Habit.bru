meta {
  name: Update Habit
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/habits/:id
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Updated Habit Name",
    "is_active": false
  }
}
