import type { ReactNode } from 'react';

/**
 * Placeholder layout component.
 * Will be fully implemented in task 2.4 with header, sidebar, and bottom nav.
 */
export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
